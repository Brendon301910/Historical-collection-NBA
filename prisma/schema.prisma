generator client {
    provider = "prisma-client-js"
}

datasource db {
    // provider = "sqlite"
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model player {
    id            String @id
    name          String
    height        String
    year_of_birth Int    @map("yearOfBirth")
}
